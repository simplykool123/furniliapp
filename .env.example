# Furnili Management System Environment Variables Template
# Copy this file to .env and fill in your actual values
# SECURITY: Never commit .env to git - it contains sensitive credentials

# Database Configuration
# Get this from your Supabase dashboard or PostgreSQL provider
DATABASE_URL=postgresql://username:password@host:port/database

# Telegram Bot Configuration  
# Get from @BotFather on Telegram - REGENERATE if compromised
# Leave empty if not using Telegram bot functionality
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# JWT Authentication Secret
# Generate with: openssl rand -hex 32
# CRITICAL: Use a secure random string (minimum 32 characters)
JWT_SECRET=your-very-secure-random-jwt-secret-key-minimum-32-characters-long

# Server Configuration
NODE_ENV=development
PORT=5000

# File Upload Paths (Local Storage)
UPLOAD_PATH_PRODUCTS=uploads/products/
UPLOAD_PATH_RECEIPTS=uploads/receipts/
UPLOAD_PATH_DOCUMENTS=uploads/documents/
UPLOAD_PATH_TELEGRAM=uploads/telegram/
UPLOAD_PATH_WHATSAPP=uploads/whatsapp/

# WhatsApp Bot Configuration (Optional)
WHATSAPP_SESSION_PATH=/tmp/whatsapp-session
CHROME_USER_DATA_DIR=/tmp/chrome-whatsapp-bot